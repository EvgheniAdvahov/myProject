spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/myproject
    username: evad
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true