spring:
  datasource:
    url: jdbc:mysql://localhost:3305/sqlFinalProject
    username: user
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true